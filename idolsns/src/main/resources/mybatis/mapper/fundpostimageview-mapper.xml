<?xml version="1.0" encoding="UTF-8"?>

<!-- dtd -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="fundPostImageView">

	<select id="selectOne" resultType="FundPostImageViewDto"
									parameterType="Long">
		select * from fundpostimage_view where post_no = #{postNo} 
	</select>

</mapper>
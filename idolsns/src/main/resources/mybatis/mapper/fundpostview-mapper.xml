<?xml version="1.0" encoding="UTF-8"?>

<!-- dtd -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="fundPostView">

	<select id="selectList" resultType="FundPostViewDto">
		select * from fundpost_view order by post_time desc
	</select>

</mapper>